#pragma once

/**
 * @file on9log_transport.h
 * @brief Constants shared by typed on9log transport sinks.
 */

/** @brief Maximum unescaped payload bytes in one typed transport frame. */
#define ON9LOG_TRANSPORT_MAX_PAYLOAD 3072u
